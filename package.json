{
  "name": "mecha_agent_inference_client_monorepo",
  "private": true,
  "scripts": {
    "build:dev": "turbo run build:dev",
    "build:prod": "turbo run build:prod",
    "dev": "turbo run dev",
    "dev:watch": "turbo watch dev",
    "release": "turbo run build:prod && changesets publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.4",
    "turbo": "^2.5.3",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}